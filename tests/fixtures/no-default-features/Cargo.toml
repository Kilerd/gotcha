[package]
name = "no-default-features"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[features]
openapi = ["gotcha/openapi"]

[dependencies]
gotcha = { path = "../../../gotcha", default-features = false }
