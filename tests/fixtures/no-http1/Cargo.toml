[package]
name = "no-http1"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[features]
openapi = ["gotcha/openapi"]

[dependencies]
gotcha = { path = "../../../gotcha", default-features = false }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
