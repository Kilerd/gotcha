[package]
name = "renamed-derive-application"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
schema = { package = "gotcha", path = "../../../../gotcha", features = ["openapi"] }
schema-library = { path = "../core-only" }
